<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <a href="/products/create">Add New Product</a>
    <ul>
        <% for(var i = 0; i < list.length; i++) { %>
            <li>
                <%= `Id: ${list[i].id}\tName: ${list[i].name}\tPrice: ${list[i].price} ` %>&nbsp;
                <a href="/products/edit/<%= list[i].id %>">Edit</a>
                <a href="/products/delete?id=<%= list[i].id %>">Delete</a>
            </li>
        <% } %>
    </ul>
</body>
</html>