<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <a href="/orders/create">Add New Order</a>
    <ul>
        <% for(var i = 0; i < list.length; i++) { %>
            <li>
                <%= `Id: ${list[i].id}\tCustomer Id: ${list[i].customerId}\tProduct Id: ${list[i].productId}\tQuantity: ${list[i].quantity}\tTotal Price: ${list[i].totalPrice} ` %>&nbsp;
                <a href="/orders/edit/<%= list[i].id %>">Edit</a>
                <a href="/orders/delete?id=<%= list[i].id %>">Delete</a>
            </li>
        <% } %>
    </ul>
</body>
</html>